{
  "installCommand": "npm install || (cat /vercel/.npm/_logs/*.log && exit 1)",
  "build": {
    "env": {
      "NODE_VERSION": "24",
      "CI": "false"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
